{
  "name": "born-to-tell",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass assets/scss -o assets/css",
    "build:watch": "nodemon --watch assets/scss --exec \"npm run build\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^8.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
